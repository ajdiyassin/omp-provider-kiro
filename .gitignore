node_modules/
dist/
*.tsbuildinfo

# Agent/tool state
.agent/
.agents/
.ralph/
*.tgz

# Local mitmproxy capture artifacts (contain profile ARNs)
kiro-capture/

# Bundled probe artifacts + live-probe logs (contain profile ARNs / account ids)
scripts/*.mjs
logs/
